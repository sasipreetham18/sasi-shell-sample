# Step 1: Use the official Python image from Docker Hub
FROM python:3.10-slim

# Step 2: Set the working directory in the container
WORKDIR /app

# Step 3: Copy your local code into the container
COPY . .

# Step 4: Set the default command to run the script
CMD ["python", "app.py"]

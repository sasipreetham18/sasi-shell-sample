pipeline {
    agent any

    stages {
        stage('Run User Management Script') {
            steps {
                sh 'chmod +x user-management.sh'  // make it executable
                sh './user-management.sh'         // run it properly
            }
        }
    }
}


pipeline {
    agent any

    stages {
        stage('Run Shell Script') {
            steps {
                sh 'chmod +x system-info.sh'
                sh './system-info.sh'
            }
        }
    }
}

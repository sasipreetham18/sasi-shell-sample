pipeline {
    agent any

    stages {
        stage('Run Shell Script') {
            steps {
                sh 'user-management.sh'
                sh './user-management.sh'
            }
        }
    }
}
